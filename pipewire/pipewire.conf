# PipeWire configuration for Intel Tiger Lake audio
# Fixes stuttering and distortion issues

context.properties = {
    # Core settings for better audio performance
    default.clock.rate = 48000
    default.clock.quantum = 1024
    default.clock.min-quantum = 32
    default.clock.max-quantum = 2048
    
    # Memory and CPU optimizations
    core.daemon = true
    core.name = pipewire-0
    
    # Latency settings
    default.clock.allowed-rates = [ 44100 48000 88200 96000 ]
    
    # Intel specific optimizations
    default.session.suspend-timeout-seconds = 0
}

context.modules = [
    # Load required modules
    { name = libpipewire-module-rt
        args = {
            # Real-time priority settings
            rt.prio = 20
            rt.time.soft = 2000000
            rt.time.hard = 3000000
        }
        flags = [ ifexists nofail ]
    }
    
    { name = libpipewire-module-protocol-native }
    { name = libpipewire-module-profiler }
    { name = libpipewire-module-metadata }
    { name = libpipewire-module-spa-device-factory }
    { name = libpipewire-module-spa-node-factory }
    { name = libpipewire-module-client-node }
    { name = libpipewire-module-client-device }
    { name = libpipewire-module-adapter
        args = { }
    }
    { name = libpipewire-module-link-factory }
    { name = libpipewire-module-session-manager }
]
